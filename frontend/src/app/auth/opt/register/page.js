// sections
import { OptRegisterView } from 'src/sections/auth/opt';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Keyword Boom Register',
};

export default function RegisterPage() {
  return <OptRegisterView />;
}
